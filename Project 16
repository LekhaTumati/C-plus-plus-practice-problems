#include <iostream>
using namespace std;

int g_c_d(int a, int b)
{
    cout << "Enter 2 positive integers\n";
    cin >> a >> b;
    if(a > 0 && b > 0)
    {
        if(a - b == 10)
        {
            cout << 10 << endl;
        }
    }
}

int main()
{
    g_c_d(50, 40);
}
